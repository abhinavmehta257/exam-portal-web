import { Alert, AlertTitle } from "@mui/material";
import React from "react";

function StartingPractice() {
  return (
    <Alert severity="success">
      <AlertTitle>Be Ready!!</AlertTitle>
      Practice is Going to Start!!
    </Alert>
  );
}

export default StartingPractice;
